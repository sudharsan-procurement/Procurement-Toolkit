__pycache__/
*.pyc
.venv/
venv/
.DS_Store
*.xlsx

# Local AI configuration (may contain API keys) — never commit
.smartdoc/
config.json
.tmpcfg/

# Streamlit secrets (OAuth client id/secret, cookie secret) — never commit
.streamlit/secrets.toml

# Local analytics database
*.db
analytics.db
