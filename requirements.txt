# --- Core app ---
streamlit>=1.42        # >=1.42 needed for native Google Sign-In (st.login/st.user)
Authlib>=1.3.2         # required by Streamlit's OIDC auth (optional sign-in only)

# --- Document extraction (digital files only in the MVP) ---
pdfplumber>=0.11      # pulls text out of digital (non-scanned) PDFs
python-docx>=1.1      # reads .docx Word files

# --- Meaning-based alignment (local, no API key) ---
sentence-transformers>=2.7   # the local embedding model lives here
scipy>=1.11                  # optimal clause-to-clause matching
numpy>=1.24

# --- Export & tabular reconcile / procurement ---
openpyxl>=3.1         # writes the .xlsx reports / reads .xlsx
pandas>=2.0           # tabular reconcile + procurement engines (also a Streamlit dep)
jinja2>=3.0           # required by pandas Styler for the highlighted result tables
requests>=2.28        # talks to cloud AI (Gemini REST) AND to an optional local
                      # Ollama server — both over plain HTTPS, so no extra SDK and
                      # no admin rights needed (works behind a corporate proxy)

# --- Converters (PDF -> Word) ---
pdf2docx>=0.5         # converts digital PDFs into editable Word .docx
PyMuPDF>=1.24         # renders PDF pages to images (used by OCR); also pulled by pdf2docx

# --- OCR (reading scanned PDFs / images) ---
pytesseract>=0.3.10   # Python bridge to the Tesseract OCR engine (engine via packages.txt)
Pillow>=10.0          # image handling for OCR
